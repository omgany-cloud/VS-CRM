# QA Audit Status — фактическая матрица

**Дата:** 2026-08-21
**Метод:** построчная сверка с кодом (`file:line`) + прогон `server/test/*.test.js`, а не оценка «на глаз». Основа — универсальный чеклист `QA_NOTES.md` пользователя (не в репозитории, приложен в диалоге), приоритеты P0/P1 по его же Priority Order.

Статусы: **[x]** — подтверждено кодом и тестами · **[ ]** — не реализовано / реальный пробел · **[~]** — частично, есть нюанс · **[?]** — требует решения продукта, не додумано.

---

## P0

### Persistence — [~]
Реальные таблицы + рабочий CRUD (400/403/404/409) подтверждены для Funds, LP, Deals(+DD как JSON), Portfolio, Capital Calls(+line items/Payments), Documents, Tasks(ob_tasks), IC memos. Client-only хранилищ не найдено — [x]. UI не показывает success до ответа сервера — [x].
- **[ ]** 0 автотестов на persistence для IC memos и Documents.
- Contacts как отдельная сущность не существует (только `founder_contacts_json` внутри Deal) — не баг, просто нет фичи.

### Server-side RBAC — [~]
`requirePermission()` — явный deny-by-default (`server/auth.js:206-214`). 21/21 tenant-isolation+delete-guards тестов проходит.
- **[ ] 3 реальных пробела:**
  1. ~~`GET /api/uploads/:id`~~ — **[x] исправлено** (коммит `63657b3`, 21.08.2026): portfolio-портал видел любой файл тенанта по id, включая KYC/AML других компаний. Теперь scoped по `portal_portfolio_id`, LP-портал отклонён полностью, деактивированный внутренний юзер больше не может качать. 5 regression-тестов (`server/test/uploads-security.test.js`).
  2. **[ ]** `GET /api/uploads/meta` (`server/index.js:224`) — всё ещё без `requirePermission`, утечка метаданных (имена файлов, кто загрузил) не-accessFM ролям. Не трогал — требует решения, какое право уместно (риск сломать легитимный кросс-модульный Vault-агрегатор).
  3. **[ ]** `POST /api/workflow/:id/withdraw` (`server/index.js:4317`) — без проверки роли/владения шагом, в отличие от approve/reject рядом. Любой internal может отозвать чужой approval-workflow.
- Нет единой матрицы ролей/прав в `docs/` — источник истины разрознён между `rolesSeed.js` и ~96 точками в коде.

### Chinese Wall — [?] требует решения продукта
`server/chineseWall.js` реализует барьер **FM vs CF&A** (по `direction`), НЕ межфондовую изоляцию. Применяется только к onboarding/ob_clients, engagements, restricted_list, documents(category), ic_memos.
- **[ ]** Межфондовой изоляции на сервере нет вообще — `activeFundId` чисто клиентский UI-фильтр. `GET /api/lp`/`/api/deals`/`/api/portfolio` отдают весь тенант (`WHERE tenant_id = ?`, без `fund_id`), это задокументированное архитектурное решение (`server/db.js:55-59`), не забытый баг.
- Global search и все autocomplete/dropdown наследуют ту же немасштабированность.
- restricted_list/coi_registry — отдельный compliance-реестр конфликтов, не access-control механизм.
- **Вопрос к продукту:** это осознанно (весь стафф УК видит все фонды) — или нужна отдельная фича серверного scope по `fund_id`? Второе — новая разработка, не багфикс.

### Audit Trail — [~]
Пишет actor(email)/timestamp/action/entity/summary. 8/8 тестов проходят. Покрывает 7 модулей: lp_register, capital_calls, distributions, deals, portfolio, engagements, conflict_approvals.
- **[ ] Не логируется:** login/logout, изменение ролей/прав пользователя, создание/удаление пользователя, ЛЮБОЙ просмотр (GET) чувствительных данных, экспорт (клиентский, без серверного вызова), просмотр самого audit-log, весь Hedge Fund модуль, весь **VC/SPV модуль** (проверил дополнительно — `recordAudit` не вызывается ни в одном из `/api/spvs*`/`/api/portfolio/:id/rounds*` роутов).
- Отсутствуют как класс полей: IP/source, correlation ID, structured diff, actor user_id (только email).

---

## P1

### Onboarding — [~]
7 задач / 5 фаз (CF&A и FM/LP шаблоны, `js/onboarding.js:31-49`), у каждой role+SLA — но **это декларативно, сервер их не проверяет** (нет 403 по несоответствию `req.user.role` и `task.role`, кроме одного литерального `role === 'RELATIONSHIP_MANAGER'` на reopen).
- **[ ]** Блокировка активации — только клиентская (`submitObTask()`, js/onboarding.js:3012). Прямой `PUT /api/ob-clients/:id {activated:true}` пройдёт без проверки завершённости остальных 6 задач.
- **[ ]** Autosave черновика — только `localStorage`, не сервер. Недоступен с другого устройства, теряется при очистке кэша.

### KYC/AML — [~]
Схема `ob_clients` полная (kyc_status, risk_rating, pep_status, sanctions_cleared, professional_client_verified, id_document_expiry).
- **[ ]** Нет ролевого гейта на изменение KYC-полей — только generic `internal`, не именно CO/MLRO.
- **[ ]** Просроченный/непройденный KYC НЕ блокирует ни одну бизнес-операцию (Capital Call для LP с непройденным KYC создаётся свободно — `server/index.js:1219-1229` фильтрует только по `status='Active'`, не по kyc). `allKycOk` — чисто дисплейный индикатор.
- **[x]** Напоминания есть: `checkKycRenewals`/`checkDocumentExpiry` (`server/notifications/digestChecks.js:28-47,128-147`), digest за 14 дней до истечения.

### Classification «2-of-3» — [ ] реальная дыра, подтверждено
Расчёт (`obClassScoreInit`, js/onboarding.js:4010-4132) — **чисто визуальная подсказка**, ни на что не влияет. Поле выбора `f_proposedClass` — обычный `<select>`, RM может выбрать любое значение независимо от результата скоринга. Сервер (`PUT /api/ob-clients/:id`, index.js:3517-3527) делает простой merge присланного JSON в БД — **никакого пересчёта из булевых критериев нет**. Итог классификации подменяем как через UI, так и прямым API-запросом без единого совпавшего критерия.
- **[ ]** Ручной override (opt-up/opt-down) — без отдельного permission, без обязательной причины, без записи в audit_log (`ob_clients`/`ob_tasks` вообще не пишутся в audit_log — 0 вызовов `recordAudit`).

### LP Register — [~]
- Единый реестр, связи с fund_id/документами подтверждены.
- **[x] Важная поправка к более раннему выводу:** `lp_register.calledAmount/paidAmount/distributions` **на самом деле НЕ статичные поля** — есть `withLiveFinancials()` (`server/lpMapping.js:100-124`), которая на каждом чтении (GET list/POST/PUT, LP-портал, MCP, external API — все 7 call site'ов проверены) пересчитывает их live-суммой из `capital_call_line_items`/`distribution_line_items`, игнорируя то, что прислал клиент. Это специально спроектированный «единый источник правды», подтверждён тестом `lp-live-financials.test.js` («ignore caller-supplied ... — always live-computed»). Хорошо сделано, никакого рассинхрона агрегатов с карточками.
- **[ ]** DELETE `/api/lp/:id` (index.js:1133-1141) проверяет footprint только в `capital_call_line_items`, **не в `distribution_line_items`** — LP с историей дистрибьюций, но без capital call, теоретически можно удалить, оставив осиротевшую ссылку `distribution_line_items.lp_id`. `POST /api/distributions` строит line items pro-rata по всем `status='Active'` LP независимо от истории capital call — путь реалистичен.
- **[x]** Поиск (`js/lp-register.js:165-170`) реально работает — case-insensitive substring по `name`/`registerId`, только эти 2 поля (нет по email/tax_id/contact). Фильтр — только по `status` (dropdown).
- **[ ] Экспорт `exportLPRegister()` (`js/export.js:80-104`) не масштабирован по фонду и без колонки фонда** — выгружает `lpRegister` целиком (весь тенант), не отфильтрованный текущий список `filtered`/`activeFundId`. При нескольких фондах в одном тенанте в один Excel-файл смешиваются LP разных фондов без возможности их различить.
- **[ ] Реальный баг данных в экспорте:** колонка «Инвестировано ($M)» (`js/export.js:94,108`) буквально дублирует «Commitment ($M)» — `lp.commitment / 1e6` вставлено в оба места, `totalInvested = totalCommit`. Выгруженная цифра «инвестировано» вводит в заблуждение — это не отдельный показатель, а copy-paste commitment.
- **[ ] UI-баннер вводит в заблуждение: «Прямое добавление в обход KYC/AML не допускается» (`js/lp-register.js:227-234`) — сервер это не проверяет.** `POST /api/lp` (`server/index.js:1013-1055`) требует только `name` и право `accessFM`; никакой проверки, что `ob_client_id` ссылается на реально активированного через онбординг клиента, нет. Любой accessFM-пользователь может создать LP напрямую через API (или теоретически через форму, если она есть в обход онбординга), полностью минуя весь KYC/AML workflow, вопреки тому, что заявляет баннер.

### Ownership — [x]
- ~~Сумма `ownership_pct` по LP фонда НЕ валидируется на сервере вообще — обычное `TEXT`/`REAL` поле, пишется как прислано (`server/index.js:1053,1096`). Может уйти за 100% без единой проверки.~~ — **[x] исправлено**: новый `validateFundOwnershipPct()` (`server/index.js`) суммирует `ownership_pct` всех остальных LP того же `fund_id` (кроме себя — при update) и отклоняет запрос 400, если сумма превысит 100% (с эпсилон-допуском на плавающую точку). Применено и к `POST /api/lp`, и к `PUT /api/lp/:id`. LP без `fundId` не валидируется. Тесты: `server/test/ownership-guard.test.js` (5 тестов: граница ровно 100%, отказ при создании сверх 100%, отказ при обновлении сверх 100%, исключение самого себя при update, LP без fundId не проверяется). Полный набор: 199/199.
- История ownership не сохраняется — PUT перезаписывает текущее значение без версионирования (это отдельный факт, не обязательно баг для этого масштаба приложения).
- Beneficial ownership и согласованность с KYC — отдельной проверки нет.

### Capital Calls + Payments — [~] (полный отчёт агента, самый детальный)
- **[x]** Жизненный цикл Draft→Pending(`ccApprove`, CEO/CFO)→Completed, уведомление LP при отправке, дедуп через `notification_log`.
- **[x]** Pro-rata по живому `commitment` на момент создания, снимок в line items не пересчитывается при последующих изменениях commitment — корректно.
- **[ ]** Валидация на переходе Draft→Pending почти отсутствует — нет проверки `totalAmount>0`, дат; только гейт на права.
- ~~Переплата (`paid > called`) не валидируется вообще~~ — **[x] исправлено** в обоих роутах (fund-level и SPV-mirror): `paid` теперь обязан быть конечным неотрицательным числом и не превышать `called` этой строки; иначе 400. Легитимная переплата сверх заявленного — это новый Capital Call/корректировка, а не большее число в этом же поле. 5 новых тестов (`server/test/overpayment-guard.test.js`).
- **[ ] Партиальная оплата после Paid не защищена** — повторный `PUT .../line-items/:lpId` на уже-`Paid` элементе НЕ требует `paymentConfirm`/wireRef заново (гейт срабатывает только на переходе `!== Paid → Paid`), можно тихо подменить сумму задним числом без evidence.
- **[ ]** Overdue — только клиентский UI-бейдж, никогда не пишется на сервер, не влияет на бизнес-логику.
- **[x]** LP-портал изоляция подтверждена тестом (LP видит строго свою line item).
- **[ ]** Округление pro-rata (`(commitment/totalCommit)*totalAmount`) без корректировки последнего LP — сумма по строкам может не совпадать с `totalAmount` на копейки.
- **[ ]** Versioning документа (Capital Call Notice) отсутствует — перегенерация после правки данных даёт другой контент под тем же номером без следа.
- **[x]** due date (`capital_calls.payment_date`) vs факт (`capital_call_line_items.payment_date`) — разведены по смыслу, но названы одинаково `paymentDate` во внутреннем API (только LP-портал переименовывает в `due_date`/`paid_date`) — источник потенциальной путаницы для интеграторов.
- 29/29 существующих тестов зелёные.

### Deal Pipeline + DD + IC — [x] один из лучше всего защищённых модулей
Полностью закрыл этот раздел — исправляю более раннее предположение «нет серверной валидации переходов». Это качественно спроектированная часть системы.
- **[x] Реальный серверный гейт переходов** (`validateStageTransition`, `server/index.js:3277-3300`): нельзя закрыть сделку без одобрения IC и подписанных документов; нельзя перейти в IC Review без `gp_conclusion_verdict === 'Рекомендовано к IC'`; нельзя перейти в Term Sheet/Переговоры без одобрения IC; и т.д. — не просто UI-подсказка, применяется на сервере.
- **[x] `ic`/`icDecision` нельзя подделать через `PUT /api/deals/:id`** (`server/index.js:3342-3353`) — эти поля **прямо заблокированы** на этом роуте («Forbidden: ic/icDecision can only be set by a resolved IC vote»); единственный легитимный писатель — серверный derive внутри `PUT /api/ic-memos/:id` после реально разрешённого голосования. `gp_conclusion_*` поля тоже отдельно защищены правом `authorICMemo` (`:3334-3341`).
- **[x] Значит DELETE-guard для deals (только по footprint `ic_memos`, `:3379-3385`) на самом деле достаточен** — раз `ic_decision='Одобрено'` физически не может появиться без настоящей записи в `ic_memos`, «закрытую» сделку без ic_memos-следа получить нельзя, удалить закрытую сделку в обход истории — тоже.
- **[x] IC-голосование хорошо защищено** (`server/index.js:4053-4096`): роль голоса неизменяема, право голосовать проверяется по доверенной серверной `prev.role` (не по присланному), нельзя перелейблить чужую пустую строку под себя, memo блокируется от правок сразу после resolution (status !== 'pending' → 409 на новые голоса/риск-заключение).
- **[x] Кворум/резолюция считаются сервером** (`deriveIcResolution`, index.js:4000-4029), не доверяя клиенту, с явной защитой от преждевременного большинства без Independent Member.
- **[x] DD red flags — подтверждено, чисто рекомендательные.** `ddRedFlags` (`js/app.js:1447-1452,2343`) — простой массив строк, рендерится как список предупреждений (⚠), нигде не используется как условие блокировки. Реальный гейт — только ручной `gp_conclusion_verdict` (человеческое суждение под правом `authorICMemo`), не автоматическая проверка находок. Стоит подтвердить у продукта, ожидался ли именно такой (человеческий) гейт, а не авто-блокировка по красным флагам.
- **[x] Структура DD**: 7 категорий (`DD_CONCLUSION_CATEGORIES`, `js/app.js:1762-1770`: Legal/Financial/Tech/Commercial/Risk/Compliance/MLRO), каждая — `conclusions[]` с `{category, author, text, verdict, documents, updatedAt}`.
- **[ ]** Тестов на deals/ic-memos кроме generic tenant-isolation/audit-log/delete-guards нет — вся эта сильная логика непокрыта регрессионными тестами. Единственный реальный пробел в этом разделе.

### Portfolio Monitoring — [~]
- **[x]** `financials_json` содержит структуру для KPI/covenants/overdue, реально сохраняется через `PUT /api/portfolio/:id`.
- **[ ] Два независимых, несинхронизированных статуса:** `portfolio.status` — хранимое поле, меняется вручную (`portChangeStatus`); `portAutoStatus()` (`js/app.js:2592-2605`) — **чисто клиентский** расчёт из `overdueAmount`/`paymentSchedule`, никогда не пишется обратно в БД. Они могут противоречить друг другу, и авто-расчёт виден только тому, кто открыл страницу.
- **[ ] Нет digest-проверки на просроченную отчётность/covenant breach** — грепнул `server/notifications/digestChecks.js` целиком на `overduePayment`/`overdueAmount`/`covenant` — 0 совпадений. Компания молча становится «Problem» только в глазах открывшего страницу, никто не уведомляется.
- **[x]** `notifyOnce()` (`server/notifications/notify.js:22-44`) — надёжный дедуп-механизм (уникальность по tenant+event+entity+recipient, `daily`-скоуп по дате), try/catch не даёт одному сбою уронить остальные уведомления в цикле — подтверждено как системный паттерн, не единичный случай.

### Automatic tasks — [ ] не идемпотентно, подтверждено
- **[x]** Нет автоматического серверного создания `ob_tasks` как побочного эффекта других действий — единственная точка создания это `POST /api/ob-tasks` (`server/index.js:3755-3779`), явный вызов от клиента с телом `{clientId, tasks[]}` (клиент сам присылает весь шаблон из 7 задач).
- **[ ] Не идемпотентно — реальный дефект.** Схема `ob_tasks` (`server/db.js:455-469`) не имеет `UNIQUE(client_id, task_num)` или любого другого уникального ограничения. `POST /api/ob-tasks` просто вставляет все присланные `tasks[]` в цикле без проверки «уже существуют ли задачи с этими task_num для этого client_id». Повторный вызов (двойной клик, retry после таймаута, race condition) создаёт **дублирующий набор из 7 задач** для того же клиента.
- Закрытие задачи при изменении исходных данных (например, обновили `id_document_expiry` — закрывается ли связанная задача 2.2 автоматически) — не проверил, не хватило времени в рамках точечного дозаезда.

### Portfolio Monitoring — квартальная история (дополнение)
- **[x]** Дефолтная структура `financials` (`js/app.js:2414-2426`) — `quarters: []`, `revenue/ebitda/netProfit/employees: {plan: [], actual: []}` — это реально **массивы**, структурно поддерживающие историю по периодам (не единичный плоский объект, который перезаписывался бы). Но раз, как уже установлено в P0, у этих полей нет UI редактирования — это неиспользуемая структурная возможность, а не работающая фича.
- **[ ]** Нет явного поля `currency`/единицы измерения в `financials_json` — предполагается $M неявно, как и весь остальной портфельный учёт в приложении.

---

## Data Integrity

### Duplicates — [ ] не исправлено, требует бизнес-решения
`UNIQUE` в `server/db.js` есть только на `tenants.slug`, `users(tenant_id,email)` (+глобальный индекс), `roles(tenant_id,code)`, `first_closing(tenant_id,fund_id)`, `hf_investor_positions(tenant_id,fund_id,lp_id)`. **На `funds`/`lp_register`/`deals`/`portfolio` — НИ ОДНОГО UNIQUE constraint** ни на name/email/registerId/taxId/BIN/company, только `id`. POST-роуты (`/api/funds`, `/api/lp`, `/api/deals`, `/api/portfolio`) проверяют только непустоту обязательного поля и сразу `INSERT` — два одинаковых запроса создают два полностью раздельных дубликата.
Импорта (CSV/Excel) в приложении нет вообще — пункт неприменим.
Сознательно НЕ добавлено сейчас: какая комбинация колонок реально означает «дубликат» — бизнес-вопрос (например, разрешены ли два Deal с одинаковым `company`, если сделка была отклонена и подана заново?), не техническая находка — по методологии этого аудита требует подтверждения бизнес-критерия, а не угадывания. Двойной клик как непосредственный источник дублей закрыт отдельно (см. Double-click ниже) без изменения схемы.

### Double-click / повторная отправка — [~] частично исправлено
- ~~На фронтенде кнопки отправки НЕ блокируются на время запроса... Реальный двойной клик по любой из них создаст два одинаковых Fund/Deal/Portfolio-company/Capital Call~~ — **[x] исправлено для 4 подтверждённых функций**: `saveFund()` (`js/funds.js`), `saveDeal()`/`savePortfolio()` (`js/app.js`), `saveIndividualCC()` (`js/lp-register.js`) теперь имеют module-level in-flight guard (`_saving*` флаг, `if (_saving*) return;` в начале, `finally { _saving* = false }`) — второй клик/повторный вызов во время активного `await apiFetch(...)` игнорируется, а не создаёт второй идентичный объект. Регрессионных тестов нет (эти функции — чистый DOM-код без headless UI test runner в проекте), проверено статическим анализом + `node -c` на все 3 файла + полный набор 200/200 не задет.
- **[ ]** Idempotency-Key механизма на сервере всё ещё нет нигде — grep по всему `server/` не нашёл ничего, кроме уже известного email-дедупа `notification_log`. Фронтенд-гард выше защищает только от буквального двойного клика в одной вкладке; повторный запрос с уровня API/двух вкладок/retry-логики клиента всё ещё создаст дубликат. Не закрыто.

### Concurrent editing — [ ]
**Optimistic locking не реализован нигде.** `PUT /api/deals/:id`, `/api/lp/:id`, `/api/portfolio/:id` — везде `Object.assign(existing, body)`/spread + безусловный `UPDATE`, без сравнения версии/`updated_at`. В схеме таблиц нет даже колонки `version`. Чистый last-write-wins — второй редактор молча затирает изменения первого без предупреждения. Это ожидаемо для PoC такого масштаба, но зафиксировано как факт, не предположение.

### Массовые операции / фоновые job'ы — [x]
`scheduler.js` оборачивает каждый digest-check в свой `try/catch` (падение одной проверки не блокирует остальные 7 и не блокирует следующий тенант), `notifyOnce()` тоже никогда не бросает — на уровне «одна ошибка не рушит батч» всё сделано правильно. Но: **[ ]** ошибки просто теряются в `console.error`, без retry-очереди и без surfaced-отчёта администратору о частичном сбое.

### Historical data — [ ] найден реальный retroactive-баг
- ~~Ретроактивная несправедливость waterfall~~ — **[x] исправлено**: `distributions`/`spv_distributions` получили новые nullable-колонки `preferred_return_snapshot`/`carried_interest_snapshot`/`catch_up_pct_snapshot`, заполняемые из текущих условий фонда/SPV в момент создания распределения. `replayWaterfallState()` (`server/waterfallEngine.js`) теперь реплеит каждое прошлое распределение по его СОБСТВЕННОМУ снимку условий, а не по текущим условиям фонда; для распределений, созданных до этой миграции (снимок = NULL), поведение не меняется — используются текущие условия фонда, как и раньше (честно, а не придуманная задним числом точность). 2 новых unit-теста с явным контрастом «должно НЕ совпасть со старым багованным значением» + 182/182 полного набора (все существующие интеграционные тесты на `POST /api/distributions` с `profitAmount` подтверждают, что проводка колонок не сломалась).
- ~~Реверс Paid→Pending обходит evidence-гейт~~ — **[x] исправлено**: `PUT /api/capital-calls/:id/line-items/:lpId` и SPV-версия теперь требуют `paymentConfirm` + непустой `reason` при ЛЮБОМ уходе со статуса `Paid`, не только при входе в него; оба направления (confirm/reverse) пишутся в `audit_log` (действие `payment_confirmed`/`payment_reversed` — этот роут раньше вообще не аудировался). 5 новых тестов (`server/test/payment-reversal.test.js`). **Не тронуто:** блокировка правок line items на `Completed`-звонке (шире, чем эта конкретная находка — отдельный вопрос про редактируемость закрытых Capital Call целиком, не только payment-статуса).

---

## Security

### Frontend XSS — [ ] **реальная уязвимость, не гипотеза**
`escapeHtml()` (js/users.js:17-24) используется последовательно почти везде — но **не в модалке создания/редактирования клиента онбординга**:
- **`js/onboarding.js:5046`** — заголовок модалки: `hdr.innerHTML = ... + client.name` без экранирования.
- **`js/onboarding.js:5082`** (присваивается через `innerHTML` на **`:5161`**) — `client.name` вставлен прямо в HTML-атрибут `value="..."` инпута без экранирования. `name`, содержащее `"><img src=x onerror=...>`, выходит из атрибута и исполняется как только сотрудник откроет форму редактирования этого клиента.
- Тот же файл использует ВТОРОЙ, менее строгий helper `esc()` (js/onboarding.js:4428) параллельно с `escapeHtml()` — `esc()` не экранирует кавычки, архитектурная непоследовательность, а не единичная опечатка.
- **Практическое влияние:** учитывая, что JWT лежит в localStorage в открытом виде (см. Session ниже) — это классическая связка XSS + token-in-localStorage = кража сессии любого сотрудника, открывшего карточку заражённого клиента. Клиента (со злонамеренным именем) может завести кто угодно с правом на онбординг.

### Authentication / Account enumeration — [ ]
- **[ ] Account enumeration подтверждена на реальном пути логина** (`POST /api/auth/login`, `server/index.js:308-332`): несуществующий email → `"Unknown tenant or user"` **без** вызова `bcrypt.compareSync` (быстрый ответ); существующий email + неверный пароль → `"Invalid email or password"` **с** вызовом bcrypt (заметно медленнее). Разные сообщения + разное время ответа — оба сигнала выдают, существует ли аккаунт. Фронтенд не передаёт `tenant`, так что это реальный, используемый путь, не побочная ветка.
- ~~Lockout на уровне аккаунта отсутствует~~ — **[x] исправлено**: новые колонки `users.failed_login_attempts`/`locked_until`. После 5 неверных попыток аккаунт блокируется на 15 минут (то же окно, что у IP rate limit — переиспользует тот же `AUTH_RATE_LIMIT_WINDOW_MS`), с отдельным сообщением «Account temporarily locked...» (тот же прецедент, что уже был у «Account is deactivated» — не новый вектор enumeration). Успешный логин сбрасывает счётчик. IP rate limit (10/окно) остаётся отдельным, независимым слоем защиты. 4 новых теста (`server/test/account-lockout.test.js`, каждый на своём выделенном сервере — иначе шарили бы IP-бюджет с самим rate limiter'ом и ломали друг друга). 189/189.
- **[x]** Пароль не в URL/query, не логируется на сервере.
- **[~]** Step-up (повторный ввод пароля) есть только для смены собственного пароля — не для создания API-ключей, удаления записей, генерации portal-паролей.

### Session — [ ]
- ~~`POST /api/auth/logout` не существует; смена/сброс пароля не инвалидирует токены~~ — **[x] исправлено**: новая колонка `users.token_version`, вшивается в JWT при выдаче (`signToken`), сверяется на каждом запросе в `requireAuth`. Теперь есть реальный `POST /api/auth/logout` (бампит версию — токен умирает на следующий же запрос); смена своего пароля и админский сброс чужого тоже бампят версию. Это **logout-everywhere** (единый счётчик на пользователя, не per-device — в приложении нет таблицы сессий для более точной отзывки), но это строгое улучшение по сравнению с «вообще ничего не инвалидируется». Смена своего пароля возвращает свежий токен в ответе — сессия не рвётся посреди работы; фронтенд (`js/app.js`, `js/api-auth.js`) обновлён, чтобы его подхватывать. Обратная совместимость: токен без claim'а `tokenVersion` (выпущенный до фикса) трактуется как версия 0 — не рвёт уже открытые сессии в момент деплоя. 3 новых теста (`server/test/session-invalidation.test.js`) + обновлены 7 существующих тестовых файлов, которые меняли пароль тестового пользователя и переиспользовали протухший токен. 185/185.
- **[ ]** Нет idle-timeout короче абсолютных 12ч.
- **[x]** Деактивация аккаунта — единственное, что даёт немедленный эффект (`requireAuth` перечитывает `active` на каждый запрос).
- Refresh tokens — не применимо, архитектурно только один 12ч access-токен без ротации.

### IDOR за пределами uploads — [x] безопасно
Выборочно проверены `/api/lp/:id`, `/api/deals/:id`, `/api/portfolio/:id`, `/api/capital-calls/:id`(+line-items), `/api/ic-memos/:id` — везде `WHERE id = ? AND tenant_id = ?` последовательно, и на чтении перед мутацией, и в финальном UPDATE/DELETE.

### CORS / Headers — [x]
CORS вообще не настроен (пакет `cors` не установлен) — де-факто безопасно, браузер применяет same-origin по умолчанию. Helmet CSP (`server/index.js:81-99`) сконфигурирован осознанно, компромиссы (`'unsafe-inline'` из-за inline `onclick`) задокументированы в коде.

### CSRF — не применимо
Вся аутентификация — Bearer JWT в заголовке, cookie-based auth нигде не используется (grep по всему `server/` — 0 совпадений `res.cookie`/`cookie-parser`). Классический CSRF неприменим.

### Секреты в бандле — [x] чисто
Хардкоженных API-ключей/секретов в js/*.js не найдено.

---

## P2

### Documents / uploads — [~]
- **[ ]** `fileFilter` (`server/index.js:192`) проверяет только заявленный `mimetype`, не реальное содержимое файла (нет `file-type`/magic-bytes библиотеки в зависимостях). Можно загрузить произвольный бинарник под видом `application/pdf`.
- **[x]** Но SVG/HTML-XSS через сам загруженный файл **не проходит** — `text/html`/`image/svg+xml` не входят в `ALLOWED_UPLOAD_MIME_TYPES` (`server/index.js:168-179`), плюс helmet выставляет `X-Content-Type-Options: nosniff` (не переопределён), так что даже подделанный mimetype не заставит браузер исполнить содержимое как HTML.
- **[x]** Path traversal исключён — `stored_name` всегда серверный `crypto.randomUUID()+ext`, оригинальное имя клиента в построение пути не участвует.
- **[ ]** Осиротевший файл на диске при сбое INSERT — нет `try/catch` с `fs.unlink` ни в `POST /api/uploads`, ни в `POST /api/portal/uploads`. Минорная утечка дискового пространства, не безопасность.
- **[x]** DELETE документа отсутствует намеренно (только archive) — аудиторский след, задокументированный дизайн, не баг.

### Google Drive links — [ ] **второй реальный XSS-кластер**
- **[ ]** Формат/домен URL не валидируется на сервере вообще — `lpaUrl`/`saUrl`/`pitchDeckUrl`/`dataRoomUrl` и т.п. пишутся как произвольный TEXT.
- **[ ] XSS при рендере, несколько мест, разного уровня серьёзности:**
  - **`js/lp-register.js:553`** — САМОЕ СЕРЬЁЗНОЕ: `lp.lpaUrl` вставлен в `onclick="...('${lp.lpaUrl}',...)"` **без экранирования вообще** (не HTML-инъекция, а прямая **JS-инъекция** — одинарная кавычка в `lpaUrl` обрывает JS-строковый литерал и исполняет что угодно по клику). Показательно: несколькими строками выше в этом же файле (`:475,478,485,488`) то же самое поле корректно экранируется через `escapeAttr()` (`js/vault.js:466-468`) — защита пропущена непоследовательно, не архитектурно отсутствует.
  - `js/app.js:1329,1333` — URL-поля вставлены в `value="..."` и `onclick="..."` без/с неполным экранированием (только одинарная кавычка эскейпится вручную, двойная — нет, а атрибут обрамлён двойными).
  - `js/onboarding.js:3540,3548,3553` (`_obOpenPreviewModal`, общая точка превью для LPA/SA/pitch deck и т.п.) — `href`/`src`/`href` из сырого пользовательского URL без экранирования, распространяется на все поля, использующие эту модалку.
  - Вектор — только для внутренних `accessFM`-пользователей (поля пишут только они), т.е. это горизонтальная/вертикальная эскалация внутри персонала, не открытый анонимный вход.
- `javascript:`-схема как отдельный вектор тоже не блокируется — ни один рендер не проверяет схему URL (`http(s):`/`mailto:` whitelist).

### Input/numeric/date validation — [ ]
- **[x]** `totalAmount` у Distributions (fund и SPV) — единственное денежное поле с реальной серверной проверкой `<= 0` (`server/index.js:1482-1483`, `:3041-3042`).
- **[ ] Всё остальное денежное — без проверки:** `totalAmount` Capital Call (fund и SPV), `commitment` LP, `invested`/`value` Portfolio, `targetSize` Fund — принимают отрицательные числа, `NaN`, произвольно большие значения и даже строки (SQLite сохранит как есть, ломая последующую арифметику pro-rata).
- **[ ]** Даты — **валидации нет нигде** (0 совпадений `isValidDate|Date.parse|dateRegex` по всему `server/index.js`). Ни формата, ни логической последовательности (`noticeDate <= paymentDate`) сервер не проверяет.

---

### Search / filters — [~]
- **[x]** Поиск везде через `.toLowerCase().includes()`, кириллица работает корректно, SQL injection структурно невозможен (весь поиск клиентский, серверных text-search эндпоинтов с построением SQL из пользовательской строки нет вообще — grep подтвердил).
- **[x]** Фильтры комбинируются (текст AND стадия/статус) там, где их несколько.
- **[ ] Реальный баг: текстовый фильтр LP Register переживает переключение фонда и реально применяется к LP нового фонда.** `switchFund()` (`js/funds.js:46-60`) вызывает `renderLPRegisterPage()` без сброса глобальных `lpRegFilter`/`lpRegStatus` — если ввести текст на одном фонде и переключиться на другой, список нового фонда молча отфильтрован старым запросом (значение видно в самом инпуте, так что не полностью скрыто, но неожиданно).
- **[~]** У Deals/Portfolio похожая, но более мягкая проблема: `switchFund()` рендерит с полными массивами (фильтр сбрасывается фактически), но `#searchDeals`/`#filterDealStage` в DOM не очищаются — поле показывает старый фильтр, который уже не применяется (визуальное рассогласование, не утечка данных).
- **[ ]** Пагинации нет нигде — все списки рендерятся целиком.

### Dashboard consistency — [~]
- **[~]** Цифры считаются реально (не заглушки), но **нет явной подписи периода/даты расчёта** рядом с KPI ("по состоянию на...", период IRR/MOIC — только в комментариях кода, не в UI).
- **[ ]** KPI-карточки на дашборде статичны — нет drill-down клика ни на одной проверенной карточке (AUM/LP count/Portfolio count).

### Modals — [~]
- **[x] Неожиданно хорошо:** предупреждение о несохранённых изменениях РЕАЛЬНО реализовано (`js/app.js:3196-3264`, snapshot-diff при закрытии модалки + `confirm()`) — это опровергает типичное для такого масштаба приложений предположение об отсутствии. Плюс `beforeunload` при открытой модалке (`js/modal-a11y.js:146-150`).
- **[x]** Ошибка API не роняет данные формы — модалка не закрывается при `catch`, только показывает toast (подтверждено на `saveFund()`/`saveDeal()`).
- **[x]** Клик вне окна закрывает — паттерн `overlay onclick="close...()"` применён последовательно у всех оверлеев.
- **[ ] 12 модалок не закрываются по Escape** — `MODAL_OVERLAY_IDS` (`js/modal-a11y.js:44-50`) содержит только 14 из существующих оверлеев; пропущены (включая часть модалок VC-модуля, добавленных в этой же сессии): `coiAddOverlay`, `distDetailOverlay`, `distNewOverlay`, `bankReconOverlay`, `hfSubNewOverlay`, `hfRedNewOverlay`, `hfNavNewOverlay`, `spvNewOverlay`, `spvDetailOverlay`, `spvInvestorNewOverlay`, `spvCcNewOverlay`, `spvDistNewOverlay`. У них же не работает focus-trap/ARIA из того же модуля.

### Responsive / browser compatibility — [x] неожиданно хорошо
Вопреки предположению «desktop-only»: есть `<meta viewport>`, 14 `@media`-блоков в `css/style.css`, реальный collapse сайдбара в off-canvas на `max-width:768px`, реколоннинг grid'ов на 1200/900/768/480px брейкпоинтах. Не desktop-only заглушка.

---

## P3

### Errors — [ ] утечка структуры БД в нескольких местах
- ~~Сырые SQLite-ошибки уходят пользователю без обёртки~~ — **[x] исправлено**: 4 подтверждённых места (`POST /api/capital-calls`, `POST /api/distributions`, `POST /api/portfolio/:id/rounds`, `POST /api/spvs/:id/distributions`) теперь логируют реальную ошибку через `console.error` и возвращают клиенту общее человекочитаемое сообщение вместо `err.message`. Остальные ~14 мест с тем же паттерном в менее часто задеваемых роутах (например `PUT`/`DELETE` transaction rollback'и) не тронуты — вне согласованного объёма этого фикса.
- **[x]** Явно написанные руками ошибки (`'purpose is required'` и т.п.) человекочитаемы, но на английском, не на русском — весь остальной UI на русском.
- **[ ]** Field-level ошибок нет вообще — ни сервер не отдаёт `field`-ключ в 400-ответах, ни фронтенд не подсвечивает конкретное поле, только общий toast.
- **[~]** Actionable-подсказки есть в delete-guard'ах («...Set status to 'Exited' instead»), но не в валидационных 400 («purpose is required» без объяснения, что дальше делать).

### Confirmations — [~]
- **[x]** `confirm()` перед всеми проверенными деструктивными действиями (LP/Deal/Portfolio/Fund/User/Engagement/CC/Distribution — 10+ мест), почти везде называет конкретную сущность по имени.
- **[ ]** Непоследовательность: `js/hf.js:318,342` (`deleteHfSubscription`/`deleteHfRedemption`) — общий `confirm('Удалить эту подписку?')` без номера, хотя соседняя функция в том же файле (`processHfRedemption:329`) уже называет номер.
- **[x]** Кастомных двусмысленных confirm-модалок нет — все 48 вызовов `confirm()` в кодовой базе нативные (чёткий OK/Cancel), включая самые опасные операции (удаление фонда, отзыв API-ключа, отзыв workflow).

### Loading states — [ ]
- **[ ]** Ни одна save-функция не блокирует кнопку отправки и не показывает индикатор ожидания на время `await apiFetch(...)` (согласуется с уже известным из Data Integrity — double-click создаёт дубли).
- **[x]** Спиннер (`fa-spinner fa-spin`) в приложении есть, но используется только для загрузки целых страниц/списков (bank-reconciliation, vault, api-keys), не для save-действий.

### Empty states — [ ] не различают «нет данных» от «нет по фильтру»
В Deal Pipeline, Portfolio, LP Register — единый текст empty-state («Нет сделок»/«Портфельных компаний не найдено»/«LP не найдено») одинаков и для полностью пустого списка, и для результата слишком узкого фильтра — вводит в заблуждение. В SPV-модуле (без текстового фильтра вообще) эта проблема структурно невозможна.

### Undo — [x]
Архивирование (Portfolio, Documents) имеет парную restore-функцию без подтверждения (архивирование — с confirm, восстановление — без, логично).

---

## Что осталось не проверено
- Automatic tasks: закрывается ли задача автоматически при изменении исходных данных.
- E2E-сценарии (FM/CF&A) — это сценарии для реального прогона (ручного или через CDP, как в модуле VC этой же сессии), не что-то вычитываемое статическим анализом кода; не входили в этот аудит.
